"""Receives pterm instance as parameter,
as wll as its arguments. This is just a
standar for all the methods ill implement,
but some of them wont use any parameters"""
def com_exit(pterm, args=None):
	pterm.setSeguir(False)

